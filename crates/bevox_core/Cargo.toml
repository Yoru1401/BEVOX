[package]
name = "bevox_core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bytemuck = { version = "1.25.2", features = ["derive"] }
glam = "0.32"

[dev-dependencies]
image = "0.25.10"

[package]
name = "bevox_render"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true

[dependencies]
bevox_core = { version = "0.1.0", path = "../bevox_core" }
bevy = "0.19.1"
bytemuck = { version = "1.25.2", features = ["derive"] }

[dev-dependencies]
pollster = "1.0.1"
wgpu = "29.0.4"
